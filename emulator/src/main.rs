mod addressing;
mod bus;
mod empty_device;
mod memory;
mod cpu;


fn main() {
    // let mut bus = Bus::new();
    // let memory = Memory::new(0x2000);
    // bus.register(memory, AddressRange::new(0, 0x1FFF));
}
